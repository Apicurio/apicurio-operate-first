apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
  # normal deployment
  # - ../base
  # multitenancy deployment
  # - ../keycloak
  # Disable fake continuous delivery
  #- ../mt_continuous_delivery
  - ../overlay_multitenancy
  - ../backstage
 # - ../housekeeping
  
